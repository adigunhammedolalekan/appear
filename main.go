package main

func main() {
	/*ser, err := git.NewService()
	if err != nil {
		log.Fatal("failed to init git service ", err)
	}

	if err := ser.CreateNewRepository("lekan", "test"); err != nil {
		log.Println("failed to create repo ", err)
	}

	s := NewServer()
	go func() {
		if err := s.Run(":8007"); err != nil {
			log.Fatal("failed to start gin server!")
		}
	}()

	log.Println("repo url ", "http://localhost:9798/lekan/test11.git")
	http.Handle("/", ser.Server)
	if err := http.ListenAndServe(":9798", nil); err != nil {
		log.Fatal("failed to start git service ", err)
	}*/
}
